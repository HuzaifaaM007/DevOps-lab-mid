name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Docker Image
        run: docker build -t huzaifamurtaza007@gmail.com/my-app:latest .

      - name: Push Docker Image
        run: docker push huzaifamurtaza007@gmail.com/my-app:latest

      - name: Deploy Container
        run: docker run -d -p 80:80 huzaifamurtaza007@gmail.com/my-app:latest 
